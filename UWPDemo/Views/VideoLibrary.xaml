<Page
    x:Class="UWPDemo.Views.VideoLibrary"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPDemo.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel Orientation="Vertical">            
            <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                <RelativePanel HorizontalAlignment="Stretch">
                    <Button x:Name="addFile" Content="+ 비디오 추가" Command="{Binding AddVideoCommand}" Margin="5" />
                    <StackPanel Orientation="Horizontal" RelativePanel.AlignRightWithPanel="True">
                    </StackPanel>                    
                </RelativePanel>
            </ScrollViewer>
            
        </StackPanel>
    </Grid>
</Page>
